[package]
name = "coco-futhark-multicore"
version = "0.1.0"
edition = "2021"

[lib]
name = "coco_futhark_multicore"
path = "../shared/src/lib.rs"

[build-dependencies]
futharkc = { path = "../futharkc" }
